---
export const prerender = false;
export const partial = true;
const settings = {
  theme: "dark",
  notifications: true,
  language: "en"
};
---

<div id="settings" class="flex flex-col items-center">
  <h1>Application Settings</h1>
  <pre>{JSON.stringify(settings, null, 2)}</pre>
</div>
